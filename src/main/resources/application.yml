server:
  port: 8080

springdoc:
  swagger-ui:
    path: /api/swagger-ui

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: postgres
  data:
    cassandra:
      schema-action: create_if_not_exists
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  servlet:
    multipart:
      max-file-size: 10MB
  main:
    allow-circular-references: true

# Show Hibernate SQL query
logging:
  level:
    org.hibernate.SQL: debug

jwt:
  secret_key: 12345
  time_expiration: 86400000

mail:
  username: "ha.levan@vti.com.vn"
  password: "xiwkamdbdhlzqbor"

cloudinary:
  url: "${CLOUDINARY_URL"

storage:
  location: upload-dir

user:
  firstName: "Doan Thi Lan"
  lastName: "Anh"
  username: "lanhxinhgai"
  password: "26113012a"
  address: "vinh phuc"
  amount: 10
  role: "ADMIN"
  email: "birtday11111@gmail.com"
  phone: "0389863041"
